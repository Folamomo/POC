clearvars;
close all;
clc;

I = imread("./parrot.bmp");
xReScale = 1.5;
yReScale = 1.5;

[YY,XX] = size(I);
nYY = floor(YY * yReScale);
nXX = floor(XX * xReScale);

nI = uint8(zeros(nYY, nXX));

for y = 1 : nYY
   for x = 1 : nXX
       nI(y, x) = I
   end
end
